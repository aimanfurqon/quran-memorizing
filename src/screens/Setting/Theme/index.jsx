import React, { useState } from "react";
import { View, Text, TouchableOpacity, StyleSheet } from "react-native";
const App = () => {
  const [currentTheme, setCurrentTheme] = useState("light");

  // Logika perubahan tema akan ditambahkan nanti
};
